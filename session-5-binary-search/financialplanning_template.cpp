#include <bits/stdc++.h>

using namespace std;


int main() {
  int n; long long M; cin >> n >> M;
  vector<long long> p(n), c(n);
  for (int i = 0; i < n; i++) {
      cin >> p[i] >> c[i];
  }
  long long min_num_days = 0;


    // your code here

  cout << min_num_days << '\n';
}
