#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;

  vector<int> can(n);
  for (int i = 0; i < n; i++) {
    cin >> cans[i];
  }

  vector<int> paint(m);
  for (int i = 0; i < m; i++) {
    cin >> paint[i];
  }

  unsigned long long waste = 0;

  // write your code here...

  cout << waste << endl;

  return 0;
}
