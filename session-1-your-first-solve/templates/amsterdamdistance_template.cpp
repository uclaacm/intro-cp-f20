#include <bits/stdc++.h>
using namespace std;

// Problem: Amsterdam Distance
// Link: https://open.kattis.com/problems/amsterdamdistance

int main() {
    int M, N;
    double R;
    int ax, ay, bx, by;
    cin >> M >> N;
    cin >> R;
    cin >> ax >> ay >> bx >> by;

    double minDis = INT_MAX;

    /*
        Do something here.
        Store your result in variable minDis
    */

    cout << setprecision(7) << minDis << endl;
}
