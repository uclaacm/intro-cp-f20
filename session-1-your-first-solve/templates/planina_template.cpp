#include <bits/stdc++.h>
using namespace std;

// Problem: Planina
// https://open.kattis.com/problems/planina

int main () {
    int N;
    cin >> N;

    int num_points;

    /*
     * Calculate the number of points stored
     * as specified by the problem
     * and store it in `num_points`
    */

    cout << num_points << '\n';
}
