#include <bits/stdc++.h>
using namespace std;

// Problem: ones
// https://open.kattis.com/problems/ones


int main () {
    int n;
    // nonstandard format: they don't tell us how many numbers
    // so we have to just wait for EOF
    while (cin >> n) {
        int ans;


        
        cout << ans << '\n';
    }
}