#include <bits/stdc++.h>
using namespace std;

// Problem: Ocean's Anti-11
// https://open.kattis.com/problems/anti11

int main() {

    int T;
    cin >> T;

    int mod = 1000000007; // output each value % mod

    vector<int> inputs(T);
    for (int i = 0; i < T; ++i) {
        cin >> inputs[i];
    }

    // your code goes here

    for (int i = 0; i < T; ++i) {
        // replace ... with the right output
        cout << ... << '\n';
    }

}