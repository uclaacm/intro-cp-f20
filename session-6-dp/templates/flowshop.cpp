#include <bits/stdc++.h>
using namespace std;

// Problem: Flow Shop
// https://open.kattis.com/problems/flowshop

int main() {
    int N, M;
    cin >> N >> M;

    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < M; ++j) {
            int tmp;
            cin >> tmp;

            // your code goes here
        }
    }

    for (int i = 0; i < N; ++i) {
        // output per swather (replace ...)
        cout << ... << ' ';
    }
    cout << '\n';
}