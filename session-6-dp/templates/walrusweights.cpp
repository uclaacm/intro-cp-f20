#include <iostream>
using namespace std;

// Problem: Walrus Weights
// https://open.kattis.com/problems/walrusweights

int main() {
    int n;
    cin >> n;

    vector<int> weights(n);
    
    for (int i = 0; i < n; ++i) {
        cin >> weights[n];
    }

    // your code goes here

    // replace ... with the right output
    cout << ... << '\n';
}