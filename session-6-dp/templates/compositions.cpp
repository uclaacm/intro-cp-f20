#include <bits/stdc++.h>
using namespace std;

// Problem: Compositions
// https://open.kattis.com/problems/compositions

int main() {
    int P;
    cin >> P;
    for (int i = 0; i < P; ++i) {
        int K, n, m, k;
        cin >> K >> n >> m >> k;

        // your code goes here

        // replace ... with the right output
        cout << K << " " << ... << endl;
    }
}