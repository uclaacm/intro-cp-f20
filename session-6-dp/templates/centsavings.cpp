#include <bits/stdc++.h>
using namespace std;

// Problem: Cent Savings
// https://open.kattis.com/problems/centsavings

int main() {
    int n, d;
    cin >> n >> d;

    vector<int> prices(n);
    for (int i = 0; i < n; ++i) {
        cin >> prices[i];
    }

    // your code goes here

    cout << ... << '\n';
}