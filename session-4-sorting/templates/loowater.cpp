#include <bits/stdc++.h>
using namespace std;

// https://open.kattis.com/problems/loowater

int main() {
    int n, m;
    cin >> n >> m;

    // while either n or m is non-zero, there's more test cases coming.
    while (n || m) {
        int heads[n], knights[m];
        for (int i = 0; i < n; i++) {
            cin >> heads[i];
        }
        for (int i = 0; i < m; i++) {
            cin >> knights[i];
        }

        int cost = 0;

        // your solution here
        // you'll have to format your output for this question

        cin >> n >> m;
    }
}