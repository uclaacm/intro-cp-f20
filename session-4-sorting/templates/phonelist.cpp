#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    for (int testcase = 0; testcase < t; testcase++) {
        int n;
        cin >> n;
        string arr[n];
        for (int i = 0; i < n; i++) {
            cin >> arr[i];
        }

        bool consistent = false;

        // your solution here

        if (consistent) {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
        }
    }
}