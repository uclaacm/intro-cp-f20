#include <bits/stdc++.h>
using namespace std;

// https://open.kattis.com/problems/dirtydriving

int main() {
    int n, p;
    cin >> n >> p;
    int distances[n];
    for (size_t i = 0; i < n; i++) {
        cin >> distances[i];
    }

    int ans = 0;

    // your solution here

    cout << ans << endl;
}