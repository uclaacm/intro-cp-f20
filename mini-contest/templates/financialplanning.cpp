#include <bits/stdc++.h>

using namespace std;

// serious overflow issues on this problem!
int main() {
  int n; long long M; cin >> n >> M;
  vector<long long> p(n), c(n);
  for (int i = 0; i < n; i++) {
      cin >> p[i] >> c[i];
  }

  long long answer;



  cout << answer << '\n';
}